# Release candidates

Flow: Change → Candidate → Validation → Evaluation → Human Approval → Canary.

Pre-release checks: tests, regression, safety, ethics, privacy, security, knowledge, multilingual consistency, and performance. Any critical check failure blocks release.

A candidate has no production side effects until every required gate passes.