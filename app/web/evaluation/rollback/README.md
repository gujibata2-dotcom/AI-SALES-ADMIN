# Rollback

Automatic rollback is triggered by configured critical safety, security, policy, or unacceptable regression thresholds. Manual rollback is available to authorized administrators.

Manual flow: select version → confirm rollback → rollback → verify → audit.

Rollback protection cannot be disabled by the AI Employee. Every rollback records actor, reason, source version, target version, verification, timestamp, and audit reference.