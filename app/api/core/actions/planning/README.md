# Action Planning

`Task → Decision → Action Plan → Risk Assessment → Authorization → Execution`

Action Plan fields: objective, actions, dependencies, risk, authorization_required, rollback_possible, verification_method.

Planning may propose actions but cannot authorize or execute them.