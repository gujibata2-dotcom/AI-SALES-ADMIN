# Action Validation

Validate schema, required fields, input types, risk, authorization, policy, idempotency, destination, and adapter capability. Invalid actions are BLOCKED. Validation never grants authority.
