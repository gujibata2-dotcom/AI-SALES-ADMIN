# Action Audit

Append-only concept covering request, validation, authorization, execution, verification, failure, retry, and rollback. Never delete records to hide failure. Prefer references over secrets/PII.
