# Facebook provider contract

Optional provider adapter. Production credentials are never stored here.

Supported capability placeholders: page_publishing, messaging, comments, media, webhooks.
Credential reference must resolve through secret management; never log credential material.
