# Credential Architecture

Never store credentials in source code, Git, fixtures, README files, logs, or examples. Future architecture: secret manager, environment variables, rotation, least privilege. Phase 14 uses placeholders only.
