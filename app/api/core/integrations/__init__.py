"""Phase 28 production integration control plane.

Provider adapters are contracts only; real credentials and network calls are
intentionally outside this phase's test/runtime fixtures.
"""
from .engine import *
