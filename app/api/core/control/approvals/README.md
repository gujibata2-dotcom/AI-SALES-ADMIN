# Approval Center

States: `PENDING`, `APPROVED`, `REJECTED`, `EXPIRED`, `CANCELLED`.

Display action, risk, reason, requester, knowledge sources, business rules and expiration. Human approval only. AI cannot approve its own action.