# Escalation Center

Queue fields: reason, priority, task, conversation reference, customer request summary, knowledge conflict, risk, required human action, created_at.

Priority is operational only and must never be used to pressure customers.