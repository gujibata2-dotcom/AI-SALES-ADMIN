# Knowledge Operations

Monitor states: `DRAFT`, `PENDING_REVIEW`, `VERIFIED`, `EXPIRED`, `REJECTED`.

Expose knowledge_id, type, source, verification, freshness, conflicts and last_verified. AI cannot publish knowledge automatically.