# Action Monitoring

Monitor action_id, task_id, action_type, risk_level, authorization, adapter, execution_status, verification, retry_count and timestamp.

Operators may inspect, retry only eligible reversible actions, and escalate failures. Irreversible actions are never automatically retried.