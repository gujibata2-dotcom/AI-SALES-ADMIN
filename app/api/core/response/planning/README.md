# Response Planning

A response plan contains `goal`, `key_points`, `required_sources`, `language`, `length`, `tone`, `cta`, `warnings`, and `restrictions`.

Price questions: answer the verified price, state relevant conditions, and optionally offer a useful next step. Do not inject unrequested promotions or pressure.
