# Response Decision

Decision types: `ANSWER`, `CLARIFY`, `RECOMMEND`, `COMPARE`, `SUPPORT`, `REFUSE`, `ESCALATE`, `SAFE_UNKNOWN`.

Decision inputs must include conversation understanding, knowledge retrieval, confidence, customer intent/emotion, business rules, ethics, safety, and human-escalation rules. Sales intent alone is never sufficient.
