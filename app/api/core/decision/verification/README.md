# Verification

After execution verify expected result, actual result, authorization, action status, customer impact, and knowledge consistency.

Mismatch results in FAILED or ESCALATE; it never silently becomes success.