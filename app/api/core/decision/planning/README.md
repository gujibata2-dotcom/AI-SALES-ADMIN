# Action Planning

Every plan defines: goal, steps, dependencies, required_knowledge, required_authorization, risk, expected_result, verification.

Example recommendation flow: understand need → retrieve verified knowledge → check constraints → compare suitable products → recommend → invite next step.