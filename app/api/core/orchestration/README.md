# Orchestration

Decision → Action Plan → Workflow → Agent → Action → Verification.

The orchestrator tracks state at every stage and cannot execute an action that lacks required dependencies or authorization.