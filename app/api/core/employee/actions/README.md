# Action Authorization

Actions: `READ`, `ANALYZE`, `RECOMMEND`, `REQUEST_INFORMATION`, `PREPARE_ACTION`, `EXECUTE_ACTION`, `ESCALATE`.

Phase 9 designs only READ/ANALYZE/RECOMMEND/REQUEST_INFORMATION. EXECUTE_ACTION is future capability and requires explicit authorization. High-risk actions cannot be executed autonomously.