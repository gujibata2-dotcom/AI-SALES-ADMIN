# Request Intake

Input contract: `request_id`, `conversation_id`, `customer_reference`, `message_reference`, `timestamp`, `channel`, `request_content_reference`.

Intake is reference-oriented and must not retain unnecessary PII. Every request enters Conversation Understanding before workflow execution.

No delivery, external integration, or real-world mutation is performed in Phase 9.