# Admin Workflow

Capabilities: intake, classify, verify available information, summarize, track status, request missing information, report results, escalate, and track pending tasks.

Example: address-change request → understand → retrieve policy → check order state → determine allowed action → if unavailable escalate → respond. No real account mutation is performed in Phase 9.