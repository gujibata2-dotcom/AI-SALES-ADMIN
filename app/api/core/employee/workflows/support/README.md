# Support Workflow

Supports `HOW_TO_USE`, `TROUBLESHOOTING`, `DELIVERY`, `RETURN`, `WARRANTY`, and `COMPLAINT`.

Flow: `Understand → Retrieve → Verify → Resolve` or `→ Escalate`.

Unsupported real-world actions remain future capabilities and require authorization.