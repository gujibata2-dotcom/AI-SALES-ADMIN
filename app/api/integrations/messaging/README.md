# Messaging Integration

Interface operations: `send_message`, `get_message`, `get_conversation`, `check_delivery_status`.

Provider implementations are intentionally absent. Phase 10 exposes contracts and uses a MockMessagingAdapter only. No real messaging occurs.