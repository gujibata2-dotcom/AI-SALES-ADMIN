# Ecommerce Integration

Interface: GET_PRODUCT, GET_PRICE, GET_STOCK, PREPARE_ORDER, GET_ORDER_STATUS.

No order is created in Phase 10. All references are synthetic and all implementations are mocks.