# Secret Policy

Secrets belong in a secure secret manager in the future. Never commit, log, return, expose tokens, credentials, or secrets.

`.env.example` may contain placeholders only. Phase 10 has no credentials.