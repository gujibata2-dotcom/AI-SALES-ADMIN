# Organizational Memory

Employee memory, team memory and organization knowledge are separate scopes. Shared knowledge must be source-aware, versioned, verified and access-controlled. Learning produces candidates; Phase 19 evaluates, approves, versions, canaries and monitors changes. No arbitrary permanent-memory writes.
