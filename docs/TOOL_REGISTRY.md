# Tool Registry

Tools declare category, schemas, risk, required permission, reversibility and external side effect. Least privilege is enforced at employee + agent + tool layers. External side effects include send, publish, payment and external updates.
