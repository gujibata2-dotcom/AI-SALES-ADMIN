# Deployment

Employee lifecycle: `DRAFT → CONFIGURED → ACTIVE → PAUSED/RESUMED → SUSPENDED → RETIRED`.

Activation requires explicit permissions. Autonomous actions use the existing authorization/governance boundary and the runtime refuses unauthorized actions.

No credentials, access tokens or provider secrets are stored by Phase 44.
