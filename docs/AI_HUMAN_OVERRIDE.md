# Human Override

Human actions may approve, reject, modify, pause, or override a decision. Store only a reference-only actor identifier plus reason and timestamp. Overrides are audit events and may feed a learning proposal; they do not silently rewrite the historical decision.
