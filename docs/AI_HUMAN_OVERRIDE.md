# Human Override

Humans may approve, reject, modify, pause or override. Every override requires a reason, timestamp and actor reference. Overrides are learning signals only after analysis and validation; they do not silently rewrite historical decisions.
