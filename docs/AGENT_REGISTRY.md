# Agent Registry

Agent records include provider, version, capabilities, supported tools/tasks, security, approval, cost and reliability. Status BLOCKED/UNAVAILABLE/DEPRECATED cannot route. Provider identity is never treated as capability evidence.
