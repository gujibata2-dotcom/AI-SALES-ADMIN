# Action Audit Policy

Record: action_id, task_id, action_type, authorization_state, adapter, result_status, verification, timestamp, error_type.

Never log passwords, API keys, payment credentials, secrets, or unnecessary PII.