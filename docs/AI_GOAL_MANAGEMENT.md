# Goal Management

Goal hierarchy: Mission → Strategic Goal → Department Goal → AI Employee Goal → Task.

Every goal should have a stable reference, parent reference, priority, and conflict/dependency information. Conflicts are surfaced rather than silently optimized away.
