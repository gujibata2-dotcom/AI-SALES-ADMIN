# AI Capability Decay

Recent performance is compared with historical performance. A meaningful decline creates CAPABILITY_DECAY and requires re-test. A single failed run never causes automatic demotion.

Requalification: Detect → Re-test → Training → Practice → Re-test → Governance decision. Model updates also route through Phase 31 regression controls and Phase 19 rollback governance.
