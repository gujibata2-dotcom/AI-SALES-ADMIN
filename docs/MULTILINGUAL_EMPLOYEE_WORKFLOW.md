# Multilingual Employee Workflow

Preserve language, locale, customer meaning, product facts, numbers, prices, units, and business rules. Cross-language handling must not alter business logic. The response language follows the customer's current language by default.