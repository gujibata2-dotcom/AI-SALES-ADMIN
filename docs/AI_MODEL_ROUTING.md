# AI Model Routing

Routing is provider-neutral. A model must satisfy capability, availability and privacy requirements before selection. Cost and reliability can rank eligible models. Fallbacks must preserve required capability and policy constraints. Ensemble agreement is not treated as truth.