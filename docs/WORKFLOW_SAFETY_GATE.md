# Workflow Safety Gate

Every workflow must pass: `KNOWLEDGE CHECK + BUSINESS RULE CHECK + PRIVACY CHECK + SAFETY CHECK + ETHICS CHECK + AUTHORIZATION CHECK`.

Failure results in `BLOCK` or `HUMAN_ESCALATION`. No workflow may bypass these gates.