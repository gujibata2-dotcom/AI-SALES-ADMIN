# AI Onboarding

Recommended flow: register → plan → business profile → employee → knowledge/rules → goals → readiness → activate.

The domain layer validates required business fields and binds every employee/task to a tenant. External documents must be treated as untrusted data; they cannot grant permissions or alter billing/policy.
