# AI Capability Discovery

Observed performance → repeated performance → candidate capability → benchmark → validation → human/governance review → registry. One-off success is an observation, not a new permanent capability.