# AI Cost Management

Cost attribution is hierarchical: organization -> project -> team -> employee -> task -> execution. Provider-disclosed price data is required for numeric attribution; otherwise status is COST_UNKNOWN. Rework, retry and human-review costs are included only when evidence exists.
