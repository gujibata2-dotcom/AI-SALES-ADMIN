# AI Process Optimization

Optimization is evidence-driven: historical workflow → performance → bottleneck → candidate → evaluation → approval → version → canary → deployment → monitoring.

Production workflow changes require the governance gate. Bottlenecks can be recorded for slow employees, tools, approvals or repeated failures. Deadlocks are detected from wait-for cycles.
