# AI Model Comparison

Models are compared only under the same benchmark, dataset version, evaluation method and scoring rules. Model names, popularity and price never substitute for evidence. Routing should consume evaluated capability evidence rather than a permanent hard-coded winner.