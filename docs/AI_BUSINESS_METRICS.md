# AI Business Metrics

Metrics contain goal linkage, definition, source, unit, target, actual, timestamp and evidence state.

Traceability target: Metric → Data → Event → Outcome. Actuals are absent when data is absent; the engine records `NO_DATA` rather than inventing a value.

ROI returns `INSUFFICIENT_DATA` unless cost, time, output and business value are all supplied.
