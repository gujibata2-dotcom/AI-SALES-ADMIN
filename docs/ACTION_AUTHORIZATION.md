# Action Authorization

Authorization order: employee permission → agent permission → tool permission → data policy → risk → side effect → human approval. No action executes before authorization. Human rejection, policy denial and invalid input are never retried.
