# Workforce Routing
Required capability → Phase 31 benchmark evidence → Phase 32 specialization/career state → permissions → availability/workload → risk → cost.

Routing must not hard-code a permanently best model or employee. If no eligible candidate exists, the task is BLOCKED or escalated.