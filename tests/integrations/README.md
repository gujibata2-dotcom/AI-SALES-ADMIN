# Mock Integration Tests

Synthetic fixtures only: mock_product, mock_customer_reference, mock_message, mock_order_reference, mock_task.

Scenarios: message send, CRM lookup, product lookup, stock lookup, order preparation, action failure, timeout, retry, duplicate action, authorization failure, human approval.

No real customer, provider, payment, order, or credential data is permitted.