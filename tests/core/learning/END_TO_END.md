# End-to-End Mock Flow

Synthetic flow only; no production side effects:

Customer Conversation → Understanding → Knowledge Retrieval → Response → Customer Feedback → Learning Event → Pattern Detection → Knowledge Gap → Recommendation → Evaluation → Human Approval → Mock Publish → Monitoring.

Expected behavior: a missing product attribute produces a knowledge-gap proposal, not an invented answer. Approval remains PENDING until a human verifies the authoritative source.
