# End-to-End Mock

Customer → Understanding → Knowledge → Decision → Orchestration → Action Gateway → Policy Gate → Authorization → Mock Adapter → Mock Execution → Verification → Control Center → Learning Engine.

Unauthorized/missing authorization BLOCK; policy violation BLOCK; duplicate DEDUPLICATE/BLOCK; payment/refund without approval BLOCK; UNKNOWN verification is not success; missing credentials BLOCK; fake provider response FLAG; retry storm trips circuit breaker; deceptive sales, unnecessary PII, secrets, and hidden profiling BLOCK.
